---
name: json-render
description: Output UI as a json-render spec using a ```spec code fence with JSONL patches for inline rendering.
---

# json-render

Use this skill when the user asks for **interactive UI**, **cards**, **forms**, **buttons**, or **structured content** that should render in chat. Do not output raw HTML.

The instructions below (generated from the catalog) define the output format, allowed components, and actions. Follow them exactly.
